// Code generated by Wire protocol buffer compiler, do not edit.
// Source: grpc.testing.ErrorStatus in grpc/testing/messages.proto
import Foundation
import Wire

public struct ErrorStatus {

    public var code: Int32
    public var message: String
    public var details: [AnyMessage]
    public var unknownFields: Data = .init()

    public init(
        code: Int32,
        message: String,
        details: [AnyMessage] = []
    ) {
        self.code = code
        self.message = message
        self.details = details
    }

}

#if !WIRE_REMOVE_EQUATABLE
extension ErrorStatus : Equatable {
}
#endif

#if !WIRE_REMOVE_HASHABLE
extension ErrorStatus : Hashable {
}
#endif

extension ErrorStatus : ProtoMessage {
    public static func protoMessageTypeURL() -> String {
        return "type.googleapis.com/grpc.testing.ErrorStatus"
    }
}

extension ErrorStatus : Proto3Codable {
    public init(from reader: ProtoReader) throws {
        var code: Int32? = nil
        var message: String? = nil
        var details: [AnyMessage] = []

        let token = try reader.beginMessage()
        while let tag = try reader.nextTag(token: token) {
            switch tag {
            case 1: code = try reader.decode(Int32.self)
            case 2: message = try reader.decode(String.self)
            case 3: try reader.decode(into: &details)
            default: try reader.readUnknownField(tag: tag)
            }
        }
        self.unknownFields = try reader.endMessage(token: token)

        self.code = try ErrorStatus.checkIfMissing(code, "code")
        self.message = try ErrorStatus.checkIfMissing(message, "message")
        self.details = details
    }

    public func encode(to writer: ProtoWriter) throws {
        try writer.encode(tag: 1, value: self.code)
        try writer.encode(tag: 2, value: self.message)
        try writer.encode(tag: 3, value: self.details)
        try writer.writeUnknownFields(unknownFields)
    }
}

#if !WIRE_REMOVE_CODABLE
extension ErrorStatus : Codable {
    public enum CodingKeys : String, CodingKey {

        case code
        case message
        case details

    }
}
#endif
